 6a.

users_list = ["alice", "bob", "alice", "charlie", "bob"]
users_set = set(users_list)

print(f"Original List (with duplicates): {users_list}")
print(f"Unique Set (duplicates removed): {users_set}")
print(f"Total duplicates found: {len(users_list) - len(users_set)}")

OUTPUT:
Original List (with duplicates): ['alice', 'bob', 'alice', 'charlie', 'bob']
Unique Set (duplicates removed): {'charlie', 'bob', 'alice'}
Total duplicates found: 2


# 6b.
def caesar(text, shift):
    result = ""
    for char in text:
        if char.isalpha():
           
            start = ord('A') if char.isupper() else ord('a')
            result += chr((ord(char) - start + shift) % 26 + start)
        else:
            result += char
    return result

msg = "Hello Python!"
encrypted = caesar(msg, 3)
decrypted = caesar(encrypted, -3)

print(f"\nOriginal: {msg}")
print(f"Encrypted: {encrypted}")
print(f"Decrypted: {decrypted}")


OUTPUT;
Original: Hello Python!
Encrypted: Khoor Sbwkrq!
Decrypted: Hello Python!

